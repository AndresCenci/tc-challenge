/**
 * Escribir el contenido de “b” al final del script.
 * function f(obj) {
 *  obj.a = 1;
 * }
 * const b = {
 *  a: 2,
 * }
 * f(b);
 *
 */

Respuesta corta: 
b = { a: 1 }

Respuesta razonada:
En esta pregunta pensé que había alguna especie de trampa porque no tiene demasiada vuelta, entonces esperaba algo de eso.
Primero tuve que releer todo el código varias veces buscando si había algún error de sintaxis o algo así que retorne error 
y luego pensé que seguro había que analizar el orden en la declaración de la función y el objeto por el tema del hoisting.
Pero no, el código hace lo que dice que hace.

